export type AdminRole = 'admin';

export type PlatformUserRole = 'guest' | 'talent' | 'vendor' | 'organizer';

export type RoleApplicationType = 'talent' | 'vendor' | 'organizer';

export type ReviewStatus = 'pending' | 'approved' | 'rejected';

export type SupportStatus = 'open' | 'in_progress' | 'resolved';

export type EventLifecycleStatus = 'active' | 'ended' | 'cancelled' | 'archived';
