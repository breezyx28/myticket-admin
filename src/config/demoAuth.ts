/** Default pre-filled credentials when `VITE_ALLOW_DEMO_AUTH` is true. Must match `AuthContext` demo email rules. */
export const DEMO_ADMIN_EMAIL = 'admin@myticket.demo';
export const DEMO_ADMIN_PASSWORD = 'password';
