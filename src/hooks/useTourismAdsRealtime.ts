/**
 * Tourism ads realtime is handled globally by useAdminRealtime in AdminShell.
 * Kept as a no-op so existing page imports do not double-subscribe.
 */
export function useTourismAdsRealtime() {
  // no-op — see useAdminRealtime
}
