/** Shared class for native `<select>` filters next to list search. */
export function filterSelectClassName() {
  return 'rounded-xl border border-ink-10 bg-white px-3 py-2 text-[13px] font-semibold text-ink outline-none focus:border-coral focus:ring-2 focus:ring-coral/25';
}
